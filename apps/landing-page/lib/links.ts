/**
 * Para onde a página leva.
 *
 * Toda chamada para ação aponta para `SIGNUP` — são seis botões, em cinco
 * dobras, e trocar o destino tem que ser uma linha e não uma caçada.
 *
 * PENDENTE: `SIGNUP` ainda é a âncora da última dobra, que é o que o arquivo de
 * design traz. Quando o cadastro do portal do cliente estiver publicado, este
 * valor vira a URL dele e os seis botões passam a levar para lá de uma vez.
 */
export const SIGNUP = "#cta";

/** Âncoras internas, usadas pelo menu e pelos links entre dobras. */
export const MODULES = "#modulos";
export const PLANS = "#planos";
export const HOW = "#como";

/**
 * PENDENTE: as três páginas do rodapé ainda não existem. Ficam como `#` — o
 * mesmo que o design traz — para não prometerem um destino que dá em nada.
 */
export const ABOUT = "#";
export const CONTACT = "#";
export const TERMS = "#";
